# Backend tests

Tests for non-UI logic: persistence (`IBookRepository` / `BookDatabase`), comic-source
loading and paging (`IComicSource`, `PdfComicSource`), and any future services.

Frontend (view model / page) tests live in `../Frontend/`. Shared test doubles live in
`../Shared/`.
